node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
expo-env.d.ts

# Native projects are generated by `npx expo prebuild` — app.json and its
# config plugins are the source of truth (health-connect manifest entries,
# minSdkVersion via expo-build-properties). Committing them would create a
# second source of truth that goes stale the next time app.json changes.
android/
ios/
